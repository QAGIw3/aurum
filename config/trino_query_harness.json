{
  "iterations": 5,
  "queries": [
    {
      "name": "curve_latest_rowcount",
      "sql": "select count(*) as total_rows from mart.mart_curve_latest"
    },
    {
      "name": "curve_fact_recent",
      "sql": "select iso_code, count(*) as rows from fact.fct_curve_observation where asof_date >= current_date - INTERVAL '7' DAY group by 1"
    }
  ]
}
