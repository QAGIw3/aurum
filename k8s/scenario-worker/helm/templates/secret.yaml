apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aurum-scenario-worker.fullname" . }}-secrets
  labels:
    {{- include "aurum-scenario-worker.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
