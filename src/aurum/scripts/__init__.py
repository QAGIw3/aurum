"""Entrypoints and helper scripts for operational workflows.

These modules are import‑safe in constrained environments (e.g., Airflow,
Kubernetes jobs) and should avoid heavy runtime dependencies.
"""
