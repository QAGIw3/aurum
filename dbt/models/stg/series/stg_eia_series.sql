with source as (
    select
        tenant_id,
        series_id,
        period,
        period_start,
        period_end,
        frequency,
        value,
        raw_value,
        unit,
        canonical_unit,
        canonical_currency,
        canonical_value,
        conversion_factor,
        area,
        sector,
        seasonal_adjustment,
        description,
        source,
        dataset,
        metadata,
        ingest_ts,
        ingest_job_id,
        ingest_run_id,
        ingest_batch_id
    from {{ source('timescale_eia', 'eia_series_timeseries') }}
)
select
    tenant_id,
    series_id,
    period,
    period_start,
    period_end,
    frequency,
    value,
    raw_value,
    unit as unit_raw,
    canonical_unit,
    canonical_currency,
    canonical_value,
    conversion_factor,
    area,
    sector,
    seasonal_adjustment,
    description,
    source,
    dataset,
    metadata,
    ingest_ts,
    ingest_job_id,
    ingest_run_id,
    ingest_batch_id
from source
