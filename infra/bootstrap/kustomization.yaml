apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aurum-dev

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: aurum-bootstrap-script
    files:
      bootstrap_dev.py=scripts/bootstrap/bootstrap_dev.py

resources:
  - job.yaml
